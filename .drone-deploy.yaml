apiVersion: apps/v1
kind: Deployment
metadata:
  name: apacheimage
  labels:
    app: image
spec:
  replicas: 1
  selector:
    matchLabels:
      app: image
  template:
    metadata:
      labels:
        app: image
    spec:
      containers:
        - name: apache-image
          image: sanket2494/apachewebserver
          ports:
            - containerPort: 8082

-----

apiVersion: v1
kind: Service
metadata:
  name: apacheimage-service
spec:
  selector:
    app: image
  ports:
  - protocol: TCP
    port: 8082
    targetPort: 8082

